flask==3.1.3
python-dotenv==1.2.1

# OpenCV — on Raspberry Pi install via apt to avoid ARM compile timeout:
#   sudo apt install python3-opencv
# On desktop/laptop:
opencv-python>=4.5

# YOLO inference — onnxruntime is ~25 MB vs PyTorch's ~600 MB
# ultralytics is only needed on the laptop to export best.pt → best.onnx:
#   python -c "from ultralytics import YOLO; YOLO('runs/detect/train-2/weights/best.pt').export(format='onnx', imgsz=640, simplify=True)"
onnxruntime>=1.17
