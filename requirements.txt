flask==3.1.3
python-dotenv==1.2.1
pyserial>=3.5          # for Arduino serial comms (optional until hardware is wired)
# opencv: on Raspberry Pi install via apt instead of pip (avoids ARM compile timeout):
#   sudo apt install python3-opencv
# On desktop/laptop:
opencv-python>=4.5     # for webcam capture via cv2.VideoCapture
